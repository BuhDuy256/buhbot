# OpenAI API Configuration -- the ONLY secret this project reads.
# The vector store is found/created by name ("optibot-kb") each run, and ENV is
# a code constant (src/config.py), so neither is configured here.
OPENAI_API_KEY=sk-proj-your-key-here
