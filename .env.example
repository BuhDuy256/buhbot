# OptiBot Pipeline — environment variables
#
# OPENAI_API_KEY : required  — your OpenAI secret key
# APP_ENV        : optional  — "production" (default in Docker) scrapes all articles;
#                              "development" caps at 30 articles for fast local runs
OPENAI_API_KEY=sk-proj-your-key-here
APP_ENV=production
